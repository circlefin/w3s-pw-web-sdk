name: 'CodeQL'
on:
  pull_request:
    branches: ['master']
  workflow_dispatch:
  schedule:
    - cron: '0 14 * * 1' # Every Monday 2PM UTC

jobs:
  run_codeql:
    if: github.repository_owner == 'circlefin'
    uses: circlefin/security-seceng-templates/.github/workflows/codeql-java.yml@v1
